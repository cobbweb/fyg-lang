module Foo

enum Option<T> {
  Some<T>,
  None
}

const yesOrNo = (value) => {
  match (value) {
    Option.Some<text> -> text
    Option.None -> 'none'
  }
}

const result = yesOrNo(Option.Some<`foo`>)
